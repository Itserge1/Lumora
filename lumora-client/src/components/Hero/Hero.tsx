"use client"

import styles from './Hero.module.css'

const Hero = () => {
    return(
        <section className={styles.hero}>
            <p>Hero Section</p>
        </section>
    )
}

export default Hero;