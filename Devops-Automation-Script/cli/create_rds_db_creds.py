def create_rds_db_credentials(db_name):
    """Function that takes a name of a db hosted on AWS and creates a credentials for it and store if on aws Secret Manager"""
    print("Creating credentials for db " + db_name)
    pass
